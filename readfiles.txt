import java.io.*;
class FileRead 
{
   public static void main(String args[])
	{
      try{
		
		FileInputStream fstream = new FileInputStream("textfile.txt");
		DataInputStream scan = new DataInputStream(fstream);
                BufferedReader br = new BufferedReader(new InputStreamReader(scan));
		String strLine;
		while ((strLine = br.readLine()) != null) 	{	
			System.out.println (strLine);
		}
		scan.close();
		}catch (Exception st){//Catch exception if any
			System.err.println("Error: " + st.getMessage());
		}
	}
}

modified in master branch
